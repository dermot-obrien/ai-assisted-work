{
  "name": "@aaw/cli",
  "version": "3.2.0",
  "description": "AI-Assisted Work CLI: bootstrap, status, claim, and run agent workflows.",
  "private": true,
  "type": "module",
  "bin": {
    "aaw": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "node build.mjs",
    "clean": "rm -rf dist",
    "test": "node --test test/install-non-interactive.test.mjs"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "@aaw/installer": "*",
    "@aaw/protocol": "*",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
