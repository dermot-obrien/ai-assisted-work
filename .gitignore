# Agents.
CLAUDE.md
/.cursor/
/.claude/
/.github/
CLAUDE.md

# Private work items (legacy — kept to protect data from pre-v2 setups).
change/work-items-private/
change/work-items-private

# Node / TypeScript build output.
node_modules/
packages/*/dist/
packages/*/node_modules/
*.tsbuildinfo

# Bundled CLI output (regenerated by `npm run build` in packages/cli).
bin/aaw.js
bin/aaw.js.map

# Bundled CLI dependency cache.
.aaw-cache/
